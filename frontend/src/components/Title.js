import styled from 'styled-components';

const Wrapper = styled.h1`
    display: flex;
    font-size: 60px;
    padding: 30px;
`

const Title = () => {
    return(
        <Wrapper>
            rePlay
        </Wrapper>
    )
}

export default Title;
